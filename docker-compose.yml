version: '3'
services:
  app:
    build:
      context: .
    volumes:
      - .:/app/.
    environment:
      - IN_DOCKER=1
    env_file:
      - .env
    depends_on:
      - selenium

  selenium:
    image: selenium/standalone-firefox:latest
    ports:
      - "4444:4444"
